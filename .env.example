# ===========================================
# Strapi CMS – for dynamic content and storing bookings
# Get STRAPI_API_TOKEN from Strapi Admin: Settings → API Tokens → Create (Full access)
# ===========================================
NEXT_PUBLIC_STRAPI_URL=http://localhost:1337
STRAPI_API_TOKEN=your-strapi-api-token


NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Note: All booking email notifications (pending, approved, rejected) are sent by Strapi.
